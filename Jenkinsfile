pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Hello World"
                
                    echo "Multiline shell steps works too"
                    
                  }
        }
    }
}
